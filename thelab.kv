WidgetsExample:

<WidgetsExample@GridLayout>:
    cols: 3
    Button:
        text: "Count"
        # on press defined inside class function in main py script
        # use 'root' instead of 'self' in this case
        on_press: root.on_button_click()
    Label:
        text: root.my_text